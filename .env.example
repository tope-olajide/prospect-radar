# Created by `npx convex dev` when a deployment is linked.
VITE_CONVEX_URL=

# Set only in the Convex deployment environment, never in the client.
# OPENAI_API_KEY=
# FIRECRAWL_API_KEY=
# AGENTMAIL_API_KEY=
